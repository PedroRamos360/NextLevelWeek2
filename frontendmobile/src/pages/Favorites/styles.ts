import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
   container: {
      flex: 1, // Para que ocupe toda a tela
      backgroundColor: '#f0f0f7',
   },
   
   teacherList: {
      marginTop: -40,
   },
});

export default styles;